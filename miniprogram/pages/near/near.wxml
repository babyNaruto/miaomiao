<!--pages/near/near.wxml-->
<search />
<view class="map">
  <map
      id="myMap"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      bindmarkertap="markertap"
      show-location
      show-compass
      show-scale
    >
    </map>
    </view>
